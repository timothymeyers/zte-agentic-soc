name: Copilot Setup Steps

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: copilot  # This must match your federated credential environment name

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 1: Authenticate to Azure with Azure CLI
      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      
      - name: Verify Azure access
        run: |
          echo "Successfully authenticated to Azure!"
          az account show

      # Step 2: Install and authenticate with AZD
      #- name: Install Azure Developer CLI (azd)
      #  run: |
      #    curl -fsSL https://aka.ms/install-azd.sh | bash
      #    azd version

      #- name: Authenticate with Azure Developer CLI (azd)
      #  run: |
      #    cd /home/runner/work/fast-api-mcp-apim-testing/fast-api-mcp-apim-testing 
      #    azd auth login --client-id ${{ secrets.AZURE_CLIENT_ID }} --tenant-id ${{ secrets.AZURE_TENANT_ID }} --federated-credential-provider github